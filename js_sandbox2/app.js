const num1 = 100;
const num2 = 50;

//simple math

val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

//math object

val = Math.PI; //gives pi value

val = Math.E; //eulers no.

val = Math.round(2.4);
val = Math.ceil(2.4);
val = Math.floor(2.8);
val = Math.pow(8, 2);
val = Math.min(2, 3, 5, 8);
val = Math.max(2, 3, 5, 8);

val = Math.random(); //this does not give random whole no

val = Math.floor(Math.random() * 20 + 1);

//the above method is used to generate a random integer between 1 and 20.

console.log(val);
